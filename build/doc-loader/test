## Button 按钮1
常用的操作按钮。
:::demo 使用`type`、`plain`、`round`和`circle`属性来定义 Button 的样式。

```html
<zm-input
  v-model="name"
  show-word-limit
  max-length='20'
>
</zm-input>
<script>
import { ref } from 'vue';

export default {
  setup() {
    const name = ref(null);
    console.log(name, '123')
    return {
      name
    }
  }
}
</script>
```
:::
